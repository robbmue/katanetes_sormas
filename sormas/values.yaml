# Default values for sormas.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

runtime: kata

metadata:
  namespace: ga-test-helm

image:
  pullPolicy: IfNotPresent
  sormas: 
    name: robbmue/sormas_kata
    version: latest
  postgres:
    name: robbmue/postgres_kata
    version: latest

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

sormas_docker_version: 2.8.0
sormas_version: 1.47.0
sormas_server_url: ga-test-helm.k8smaster
latitude: '52.2109803'
longitude: '10.5314925'
map_zoom: '10'
tz: europe/berlin