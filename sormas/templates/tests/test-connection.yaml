apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sormas.fullname" . }}-test-connection"
  labels:
    {{- include "sormas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sormas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
