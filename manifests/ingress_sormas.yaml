apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: sormas
  namespace: ga-test
spec:
  rules:
  - host: ga-test.k8smaster      
    http:
      paths:
      - path: /
        backend:
          serviceName: sormas 
          servicePort: 80
